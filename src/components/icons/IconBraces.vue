<!-- Placeholder icon — swap for the real <IconBraces/> when the icon set lands.
     Rendered as text rather than a path since "{}" is already legible at
     the tiny sizes this button is used at. -->
<template>
  <span class="inline-flex items-center justify-center font-mono leading-none" aria-hidden="true"
    >{}</span
  >
</template>